<div class="min-h-screen bg-base-200 py-12 px-6">
  <div class="max-w-4xl mx-auto">
    <header class="mb-8 text-center">
      <h1 class="text-4xl font-bold tracking-tight">Dein Warenkorb</h1>
      <p class="mt-3 opacity-75">
        Die Inhalte werden hier erscheinen, sobald die React-Komponenten an das neue Rails Backend angebunden sind.
      </p>
    </header>

    <section class="card bg-base-100 shadow-xl">
      <div class="card-body">
        <% if @cart_items.empty? %>
          <div class="py-8 text-center space-y-4">
            <p class="opacity-70">Noch keine Produkte im Warenkorb.</p>
            <%= link_to "Jetzt stöbern", marketing_products_path, class: "btn btn-primary" %>
          </div>
        <% else %>
          <div class="overflow-x-auto">
            <table class="table">
              <thead>
                <tr>
                  <th>Produkt</th>
                  <th>Store</th>
                  <th class="text-right">Preis</th>
                </tr>
              </thead>
              <tbody>
                <% @cart_items.each do |product| %>
                  <tr>
                    <td>
                      <div class="flex items-center gap-3">
                        <% if product.main_product_image.present? %>
                          <div class="avatar">
                            <div class="w-12 rounded">
                              <%= image_tag product.main_product_image, alt: product.title %>
                            </div>
                          </div>
                        <% end %>
                        <div>
                          <p class="font-semibold"><%= product.title %></p>
                          <p class="text-sm opacity-70"><%= truncate(product.description, length: 60) %></p>
                        </div>
                      </div>
                    </td>
                    <td><%= product.user&.business&.business_name || "Direkt" %></td>
                    <td class="text-right"><%= number_to_currency(product.price) %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <div class="mt-6 flex items-center justify-between">
            <span class="text-sm uppercase tracking-wide opacity-70">Zwischensumme</span>
            <span class="text-2xl font-semibold"><%= number_to_currency(@cart_total) %></span>
          </div>
        <% end %>
      </div>
    </section>
  </div>
</div>
